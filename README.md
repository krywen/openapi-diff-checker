# openapi-diff-checker
Verify if 2 openapi files are functionally equivalent (ignores comments, descriptions, etc and check for paths, data type and formats, etc).
Can be called from command line providing src and dest openapy.yaml files.

It's a python module that other people can import and use

TODO
- implement the tool
- document how to use the tool
- document how other people can get and use this tool
- document dependencies
- add tests
